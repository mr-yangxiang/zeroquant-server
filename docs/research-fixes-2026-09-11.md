# 2026-09-11 复查问题修复记录

本记录对应本机后端修复与隔离验证，未部署、未连接或修改云数据库，未升级模型或开放交易信号。后续经用户授权的 Git 提交与推送不代表线上部署或实盘验收；前后端已有工作均保留。

## 修复范围

| 问题 | 修复与验收 |
|---|---|
| 已执行 005 的旧库跳过新增字段 | 新增迁移 007，补 `stock_l2_orders.created_at`；生产执行器和测试共用迁移目录清单；005/007 回滚均不删除此基准字段 |
| 其他股票行情更新让旧持仓估值“变新” | 逐股保存价格和时间证据；缺价/陈旧持仓明确列出，保留未核验估算但不接受为收益和回撤证据 |
| 每天一条盘口即可通过覆盖检查 | 按每只股票、每天的实际决策时点检查 60 秒内可用盘口，覆盖率至少 95%，连续缺失不超过 5 个交易分钟 |
| 盘中不足 240 分钟被误判失败 | 完整历史日与进行中的当日分别检查；盘中按已闭合分钟加 60 秒到达宽限，保留对真实缺口的拦截 |

生产策略同步升级为 `production_v2`，防止旧质量结论或旧批准记录自动继承。数据质量与特征构建共享决策时间及 PIT 分钟窗口逻辑。

## 本机验证

最终结果：Python **60 项测试通过**（原有 41 项，新增 19 项）；后端 TypeScript 构建、生产门禁测试、47 张表实体一致性检查、7 个迁移的隔离数据库验证均通过。前端生产构建通过，仍有已有的包体积及第三方 PURE 注释警告。本轮未改动前端源码。

新增用例还覆盖新版策略拒绝旧批准、现金空仓、历史价格证据不可变、开盘及午后恢复等待首根完整分钟。等待窗口不写估值或成交，宽限后缺行情仍按失败处理。

```bash
cd /Users/yx/mine/project/zeroquant-server
npm run build
npm run schema:catalog-check
node --import tsx test/research-production.test.ts
PYTHONPATH=quant_engine .venv-research/bin/python -m unittest discover -s quant_engine/tests -q
```

另使用独立内存 PostgreSQL/WASM 数据库执行 `test/research-migrations-smoke.ts`，验证：全新库 001–007、47 张表字段与实体清单、已登记 001–005 旧库的增量修复、重复执行、业务数据和既有时间戳保留、回滚不删除基准字段，以及事务回滚。

隔离迁移测试需要先安装 PGlite 并将 `PGLITE_MODULE_PATH` 指向其 `dist/index.js`；不读取生产连接配置，不可替换成云库测试。

## 部署时的注意事项

1. Git 推送与线上部署是独立步骤。发布时必须包含原有研究管道、迁移 006、新增迁移 007、迁移清单及配套实体，不能只拷贝 007。
2. 线上先确认目标数据库和备份，再运行 `npm run migrate:status`、`npm run migrate:up`、`npm run schema:verify`。本次未执行这些线上写入。
3. 007 仅补缺失字段，不改变已有时间戳。旧行新增字段的默认值是修复时间，不代表可追溯的历史采集时间，禁止据此认证历史 PIT 数据。
4. 旧模型报告和影子账本保留供审计，不能手改新版策略标记，也不能删掉不合格历史点来制造合格净值。
5. 按更新后的部署手册重新核验真实数据质量、冻结实验及持续影子证据。通过软件回归不等于已证明预测准确或具备实盘资格。

详细规则见同目录 `research-pipeline.md`。本轮没有执行真实 HTTP 端到端测试、云库升级或真实样本外收益验证。

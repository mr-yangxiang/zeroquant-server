# 迁移 008：云库已执行，兼容代码随本提交交付

更新：2026-09-14。用户在已说明七表变更范围后明确要求“不需要备份，直接开始”。本次通过加密 SSH 和容器本地 Unix socket 执行了独立的 008 操作脚本，未重启应用。

## 2026-09-14 实际执行结果

- 云库已登记 008，applied_at 为 `2026-09-14T06:32:16.849Z`（这是事务时间，不是精确提交完成时刻）。
- 单事务内锁定七张业务表及迁移登记表，创建临时比较副本，迁移后逐字段双向比较确认既有记录和值保持不变；结构无错误才提交。
- 覆盖 users 17 行、stocks 6 行、stock_price_histories 79,299 行、stock_t_analyses 6 行、stock_day_predictions 330 行、stock_rolling_predictions 642,700 行、stock_l2_orders 3,001 行。
- 提交后新建只读连接复核，时间 `2026-09-14T15:04:50.677Z`：新版 47 表实体契约下 **0 错误、2 条旧时间类型警告**，001–008 版本记录齐全。
- 两张旧预测表的 created_at 未转换，历史时区仍未知；不能将兼容警告当成 PIT 证据已认证。
- 后端健康正常，systemd MainPID 仍为 3234，本轮没有发布代码或重启业务。
- 用户取消了备份流程；没有完成独立备份或恢复验证。事务临时比较表提交即删除，不是可恢复备份。
- 本提交纳入 008、实体目录、核验器及回归测试。执行云库迁移时没有同步或重启应用，彼时服务器代码为 a41616b；Git 推送不等于服务器发布，仍需部署本提交后再核验线上版本。

## 已完成

1. 修复结构核验器：识别独立唯一索引及 INCLUDE 列，不误认部分/表达式/非唯一索引；无效索引不算可用索引。
2. 新增 008 兼容迁移和对应实体定义：保留旧标识、补登记辅助字段、调整已知兼容差异；不删除业务行，不重生成已有 UUID，不转换未知历史时区。
3. 隔离 PostgreSQL 验证 ID 和关联不变、旧预测值/时间不变、新 ID 生成、重复执行、失败回滚、长度不截断。
4. 后端构建、47 表目录、001–008 隔离迁移、生产门禁及 60 项 Python 测试通过。

## 2026-09-13 历史阻塞记录（已被上面的实际执行结果取代）

9 月 13 日只读预检确认云库仍登记到 007，待保护的 8 张表及索引合计约 128 MiB，待收紧的必要字段没有发现 NULL。随后一次只读结构核验发生连接中断。

计划执行的库内快照和 008 迁移被执行环境安全审核拒绝：同库快照不是独立可恢复备份，且这次涉及主键类型、字段类型及约束调整，需要具体授权和更可靠的恢复条件。**快照和迁移均未开始，云库没有被本轮修改。**

此前没有执行成功的迁移不得手工补登记。2026-09-14 的 008 记录是在实际迁移、数据保留比对和结构检查通过后写入并提交的，不是为了通过检查而补造记录。

## 后续事项

- SSH 及本次明确写入授权均已获得，008 已执行。后续仍需建立正式备份与恢复流程；本次用户选择跳过不能当成已有灾备能力。
- 将本提交同步并发布到实际提供服务的 systemd 后端，避免线上代码清单落后于已更新的云库。部署前检查工作区和版本，部署后确认 008 跳过执行且新版结构核验通过。不要执行自动 down；不要擅自转换未知历史时区。
- 用户目前没有已授权的多年分钟、历史新闻或真实 Level-2 来源。需要取得数据许可或真实导出后才能回填、生成正式训练集；公开近期数据不能替代。
- 真实影子运行时间无法补造。代码验收不等于已具备实盘资格，正式模型到页面的生产适配和线上完整验收仍未完成。

本记录不是完整灾备方案，也不是上线批准。
